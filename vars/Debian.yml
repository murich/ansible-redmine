packages:
  - git
  - imagemagick
  - build-essential
  - rbenv
  - libmagickwand-dev
  - "{{ 'libmariadbclient-dev' if ansible_distribution_version is version('9', '>=') else 'libmysqlclient-dev' }}"
